<Page
    x:Class="StoreProjectApp.Pages.ProductListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoreProjectApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MyToolkit.Controls"
    xmlns:viewModels="using:StoreProjectApp.Presentation"
    xmlns:ui="using:Intense.UI"
    xmlns:intense="using:Intense.UI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <viewModels:ProductListPageModel x:Key="ViewModel"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{StaticResource ViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ProgressRing x:Name="Ring" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Height="100" Width="100" IsActive="True"/>
        <StackPanel x:Name="StackPanelMain" Grid.Row="0" Padding="{x:Bind Frame.Padding, Mode=OneWay}" Background="{ThemeResource SystemAcrylicWindowChromeLowBrush}" Margin="0,-33,0,0">
            <TextBlock Margin="0,33,0,16" x:Name="Title" Text="Product List" Style="{StaticResource TitleTextBlockStyle}" DataContext="{x:Bind Frame}" Visibility="{Binding PageTitleVisibility}" />
            <TextBlock Text="SEARCH" FontWeight="Bold" Margin="0,0,0,2" />
            <AutoSuggestBox Text="{Binding Filter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                    PlaceholderText="Search for a specific product..." Margin="0,0,0,16" AutoMaximizeSuggestionArea="True" QueryIcon="Find"/>
        </StackPanel>
        <controls:DataGrid x:Name="DataGrid" Grid.Row="1"
                           ItemsSource="{Binding Product}" 
                           SelectionMode="Extended" 
                           SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                           DefaultOrderIndex="3"
                           RowBackgroundEvenBrush="Transparent"
                           RowBackgroundOddBrush="Transparent"
                           Padding="{x:Bind Frame.Padding, Mode=OneWay}">
            <controls:DataGrid.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="10" Background="{ThemeResource SystemAcrylicWindowChromeLowBrush}" VerticalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" TextWrapping="Wrap" Text="{Binding Header}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <Grid Grid.Row="0" Grid.Column="1" Visibility="{Binding IsSelected}" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <StackPanel Background="{ThemeResource SystemChromeMediumColor}">
                                <TextBlock Text="" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Visibility="{Binding IsAscending}" Background="{ThemeResource SystemChromeMediumColor}">
                                <TextBlock Text="" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Visibility="{Binding IsAscending}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </controls:DataGrid.HeaderTemplate>
            <controls:DataGrid.Resources>
                <DataTemplate x:Key="AppIcon" >
                    <StackPanel Height="50" Width="50" Background="{ThemeResource SystemAccentColor}">
                        <StackPanel Height="50" Width="50" Background="{Binding BackgroundColor}">
                            <Image Height="50" Width="50" VerticalAlignment="Center" HorizontalAlignment="Center" Source="{Binding Icon}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
                <DataTemplate x:Key="Name" >
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
                <DataTemplate x:Key="Publisher" >
                    <StackPanel>
                        <TextBlock Text="{Binding Publisher}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
                <DataTemplate x:Key="LastUpdated">
                    <StackPanel>
                        <TextBlock Text="{Binding LastUpdated}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </controls:DataGrid.Resources>
            <controls:DataGrid.RowStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </controls:DataGrid.RowStyle>
            <controls:DataGrid.Columns>
                <controls:DataGridTemplatedColumn Width="100" CanSort="False" CellTemplate="{StaticResource AppIcon}"/>
                <controls:DataGridTemplatedColumn Width="*" Header="NAME" CellTemplate="{StaticResource Name}" Order="{Binding Name}"/>
                <controls:DataGridTemplatedColumn Width="*" Header="PUBLISHER" CellTemplate="{StaticResource Publisher}" Order="{Binding Publisher}"/>
                <controls:DataGridTemplatedColumn Width="*" Header="UPDATED ON" CellTemplate="{StaticResource LastUpdated}" Order="{Binding LastUpdated}" IsAscendingDefault="False"/>
            </controls:DataGrid.Columns>
            <controls:DataGrid.ItemDetailsTemplate>
                <DataTemplate>
                    <StackPanel Margin="12,0,12,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Column="0" Grid.Row="0" Margin="0,0,8,0" Text="Product ID" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding ProductID}" />
                            <TextBlock Grid.Column="0" Grid.Row="1" Margin="0,16,8,0" Text="Actions" FontWeight="Bold"/>
                            <StackPanel Grid.Column="1" Grid.Row="1" Margin="0,16,8,0">
                                <Button x:Name="DownloadButton" Margin="0,8,0,8" Content="Download Packages" Click="DownloadButton_Click"/>
                                <Button x:Name="DownloadNewButton" Margin="0,8,0,8" Content="Download Packages (Production)" Click="DownloadNewButton_Click"/>
                                <Button x:Name="DownloadNewIRPButton" Margin="0,8,0,8" Content="Download Packages (Insider Release Preview)" Click="DownloadNewIRPButton_Click"/>
                                <Button x:Name="DownloadNewMSITButton" Margin="0,8,0,8" Content="Download Packages (Microsoft)" Click="DownloadNewMSITButton_Click"/>
                                <Button x:Name="DownloadNewWISButton" Margin="0,8,0,8" Content="Download Packages (Windows Insider Slow)" Click="DownloadNewWISButton_Click"/>
                                <Button x:Name="DownloadNewWIFButton" Margin="0,8,0,8" Content="Download Packages (Windows Insider Fast)" Click="DownloadNewWIFButton_Click"/>
                                <Button x:Name="DownloadNewWIFSButton" Margin="0,8,0,8" Content="Download Packages (Windows Insider Fast Skip)" Click="DownloadNewWIFSButton_Click"/>
                                <Button x:Name="DownloadNewSHButton" Margin="0,8,0,8" Content="Download Packages (Selfhost)" Click="DownloadNewSHButton_Click"/>
                                <Button x:Name="DownloadNewCanaryButton" Margin="0,8,0,8" Content="Download Packages (Canary)" Click="DownloadNewCanaryButton_Click"/>
                                <Button x:Name="OpeninStoreButton" Margin="0,8,0,8" Content="Open in the Store" Click="OpeninStoreButton_Click"/>
                                <Button x:Name="CopyProdIDButton" Margin="0,8,0,8" Content="Copy Product ID" Click="CopyProdIDButton_Click"/>
                            </StackPanel>
                            <TextBlock Grid.Column="0" Grid.Row="2" Margin="0,16,8,0" Text="Number of downloads" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Grid.Row="2" Margin="0,16,0,0" Text="{Binding PurchaseCount}" TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="0" Grid.Row="3" Margin="0,0,8,0" Text="Number of ratings" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding RatingCount}" TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="0" Grid.Row="4" Margin="0,0,8,0" Text="Average rating" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Grid.Row="4" Text="{Binding AverageRating}" TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="0" Grid.Row="5" Margin="0,16,8,0" Text="Description" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Grid.Row="5" Margin="0,16,0,0" Text="{Binding Description}" TextWrapping="Wrap"/>
                        </Grid>

                    </StackPanel>
                </DataTemplate>
            </controls:DataGrid.ItemDetailsTemplate>
        </controls:DataGrid>
        <StackPanel Grid.Row="2" Background="{ThemeResource SystemChromeMediumColor}">
            <TextBlock Padding="8" Text="Loading" x:Name="Status" HorizontalAlignment="Right"/>
        </StackPanel>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanelMain.Margin" Value="0,-33,0,0" />
                        <Setter Target="Title.Margin" Value="0,33,0,16" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="StackPanelMain.Margin" Value="0,0,0,0" />
                        <Setter Target="Title.Margin" Value="0,0,0,16" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
