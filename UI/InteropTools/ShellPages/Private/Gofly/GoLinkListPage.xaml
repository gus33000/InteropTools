<Page
    x:Class="StoreProjectApp.Pages.GoLinkListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoreProjectApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MyToolkit.Controls"
    xmlns:viewModels="using:StoreProjectApp.Presentation"
    xmlns:ui="using:Intense.UI"
    xmlns:intense="using:Intense.UI.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <viewModels:GoLinkListPageModel x:Key="ViewModel"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{StaticResource ViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <StackPanel Margin="0,-33,0,0" Grid.Row="0" Padding="{x:Bind Frame.Padding, Mode=OneWay}" Background="{ThemeResource SystemAcrylicWindowChromeLowBrush}">
                <TextBlock Margin="0,33,0,16" x:Name="Title" Text="Go Link List" Style="{StaticResource TitleTextBlockStyle}" DataContext="{x:Bind Frame}" Visibility="{Binding PageTitleVisibility}" />
                <TextBlock Text="SEARCH" FontWeight="Bold" Margin="0,0,0,2" />
                <AutoSuggestBox Text="{Binding Filter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                    PlaceholderText="Search for a specific go link..." Margin="0,0,0,16" AutoMaximizeSuggestionArea="True" QueryIcon="Find"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Column="0" VerticalAlignment="Center" Grid.Row="0" Text="START ID" FontWeight="Bold" Margin="0,0,16,4"/>
                    <TextBox Grid.Column="1" Grid.Row="0" Margin="0,0,0,4" x:Name="StartIDTextBox" Text="0"/>
                    <TextBlock Grid.Column="0" VerticalAlignment="Center" Grid.Row="1" Text="END ID" FontWeight="Bold" Margin="0,0,16,4"/>
                    <TextBox Grid.Column="1" Grid.Row="1" Margin="0,0,0,4" x:Name="EndIDTextBox" Text="0"/>
                </Grid>
                <Grid Margin="0,16,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel HorizontalAlignment="Stretch" Grid.Column="0" Margin="0,0,2,0">
                        <Button HorizontalAlignment="Stretch" x:Name="StartButton" Content="Start" Click="StartButton_Click"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Stretch" Grid.Column="1" Margin="2,0,0,0">
                        <Button HorizontalAlignment="Stretch" x:Name="PauseButton" Content="Pause" Click="PauseButton_Click"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
            <controls:DataGrid x:Name="DataGrid" Grid.Row="1"
                           ItemsSource="{Binding GoLink}"
                           SelectionMode="Single"
                           SelectedItem="{Binding SelectedGoLink, Mode=TwoWay}"
                           DefaultOrderIndex="0"
                           RowBackgroundEvenBrush="Transparent"
                           RowBackgroundOddBrush="Transparent"
                           Padding="{x:Bind Frame.Padding, Mode=OneWay}">
                <controls:DataGrid.HeaderTemplate>
                    <DataTemplate>
                        <Grid Padding="10" Background="{ThemeResource SystemAcrylicWindowChromeLowBrush}" VerticalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" TextWrapping="Wrap" Text="{Binding Header}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Grid Grid.Row="0" Grid.Column="1" Visibility="{Binding IsSelected}" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <StackPanel Background="{ThemeResource SystemAcrylicWindowChromeLowBrush}">
                                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Visibility="{Binding IsAscending}" Background="{ThemeResource SystemAcrylicWindowChromeLowBrush}">
                                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Visibility="{Binding IsAscending}"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </controls:DataGrid.HeaderTemplate>
                <controls:DataGrid.Resources>
                    <DataTemplate x:Key="ID" >
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock VerticalAlignment="Center" Text="{Binding ID}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                    <DataTemplate x:Key="Link" >
                        <StackPanel>
                            <HyperlinkButton NavigateUri="{Binding Link}">
                                <TextBlock TextWrapping="Wrap" Text="{Binding Link}"/>
                            </HyperlinkButton>
                        </StackPanel>
                    </DataTemplate>
                </controls:DataGrid.Resources>
                <controls:DataGrid.RowStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </controls:DataGrid.RowStyle>
                <controls:DataGrid.Columns>
                    <controls:DataGridTemplatedColumn Width="100" Header="ID" CellTemplate="{StaticResource ID}" Order="{Binding ID}"/>
                    <controls:DataGridTemplatedColumn Width="*" Header="LINK" CellTemplate="{StaticResource Link}" Order="{Binding Link}"/>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
            <StackPanel Grid.Row="2" Background="{ThemeResource SystemAcrylicWindowChromeLowBrush}">
                <TextBlock Padding="8" Text="Loading" x:Name="Status" HorizontalAlignment="Right"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
