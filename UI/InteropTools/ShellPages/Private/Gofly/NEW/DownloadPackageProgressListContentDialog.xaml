<ContentDialog
    x:Class="StoreProjectApp.ContentDialogs.NEW.DownloadPackageProgressListContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoreProjectApp.ContentDialogs.NEW"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Download Packages"
    SecondaryButtonText="Close"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    Style="{StaticResource ResourceKey=MyContentDialogControlTemplate}">

    <Grid>
        <ListView x:Name="DownloadList" Margin="0,16,0,0" SelectionMode="None" HorizontalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,4,0,4">
                        <Grid HorizontalAlignment="Left">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="32"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock VerticalAlignment="Center" FontSize="32" Text="{Binding Symbol}" FontFamily="Segoe MDL2 Assets" Foreground="{ThemeResource SystemAccentColor}"/>
                            <Grid Grid.Column="1" Margin="8,0,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{Binding Name}" TextWrapping="Wrap"/>
                                <TextBlock Opacity="0.5" Grid.Row="1" Text="{Binding Type}" TextWrapping="Wrap"/>
                                <ProgressBar Maximum="100" Minimum="0" Grid.Row="2" HorizontalAlignment="Stretch" Value="{Binding Progress}"/>
                                <TextBlock Opacity="0.5"  Grid.Row="3" Text="{Binding Status}" TextWrapping="Wrap"/>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
